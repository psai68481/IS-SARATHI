# IS-SARATHI production dependencies (Vercel container runtime)
# Same feature set as requirements.txt minus services that are never used in
# production (Pinecone/Redis/boto3 have code fallbacks and stay uninstalled):
# the RAG embeddings are a native NumPy engine, retrieval is Postgres-driven.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database (Vercel Postgres / Neon - PostgreSQL)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# Security & JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20

# AI & NLP stack. spaCy + model downloaded in the Dockerfile.
spacy==3.8.3
# CPU-only torch keeps the image well under function size limits while
# sentence-transformers stays importable for the semantic layer.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
sentence-transformers==3.3.1
huggingface-hub==0.27.1
numpy==2.1.3

# AI vector database (Pinecone serverless - free tier). The RAG retrieval
# layer upserts standard embeddings at seed time and queries Pinecone for
# candidate generation; the code's in-memory vector index is the automatic
# failover when the key is absent.
pinecone-client>=3.1.0

# Tender document processing: PDF text extraction + OCR (SIH26108 stage 4)
pypdf==5.1.0
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.1.0

# HTTP & utilities
requests==2.32.3
httpx==0.28.1
python-dotenv==1.0.1
pytest==8.3.4
